# 04 — Задачи для Sonnet (реализация)

Документ для модели-исполнителя (Sonnet), которая пишет код. Задачи упорядочены: делать
строго по очереди, каждая со своими критериями приёмки. Не начинать следующую, пока текущая
не проходит критерии. Контекст — в `00..03` и `05`. Визуальный стиль и шрифты сохраняем
(Cormorant Garamond + Inter, текущая эстетика).

Общие правила:
- Стек: статический HTML/CSS/JS, без фреймворка и сборки. Хостинг GitHub Pages.
- UI-строки — в `js/i18n/ru.js` (объект-словарь), не хардкодить вразнобой в HTML.
- Supabase-ключи (anon) — в `js/config.js`. `service_role` нигде не использовать.
- Безопасность данных = RLS. Любая таблица без включённого RLS = баг.
- Все запросы к Supabase — через `@supabase/supabase-js` (CDN ESM).

---

## T0 — Подготовка проекта Supabase (ручной шаг + SQL)
**Делает:** Тёма создаёт проект на supabase.com (free). Sonnet готовит SQL-миграцию.
**Выход:**
- `supabase/schema.sql` — создание таблиц `profiles`, `projects`, `comments`,
  `project_upvotes` по схеме из `01-architecture.md`.
- Триггер: при регистрации в `auth.users` создаётся строка `profiles`.
- Триггер: пересчёт `projects.upvotes` при изменении `project_upvotes`.
- Включён RLS на всех таблицах + все политики из `01-architecture.md`.
- Bucket Storage `covers` (публичное чтение, запись для аутентифицированных).
**Критерии приёмки:**
- SQL применяется без ошибок.
- Анонимный клиент НЕ может вставить `projects` со `status='published'`.
- Анонимный клиент НЕ может писать в чужой профиль.
- Залогиненный может вставить проект только с `author_id = своему uid` и `status='pending'`.

## T1 — Подключение Supabase к фронту
**Выход:**
- `js/config.js` — `SUPABASE_URL`, `SUPABASE_ANON_KEY`.
- `js/supabase.js` — инициализация клиента (CDN ESM import).
- `js/auth.js` — `signUpEmailPassword(email, pass)`, `signInEmailPassword(email, pass)`,
  `signInMagicLink(email)`, `signOut()`, `getCurrentUser()`, подписка на изменение сессии.
  (Google не делаем — заблокирован в РФ; Telegram-логин — бэклог, см. `01-architecture.md`.)
- **Модалка логина/регистрации** — открывается по всем `[data-join]` («Вступить»).
  Почта+пароль (основной) + магик-линк. В стиле T-D. Заменяет текущий тост-заглушку.
- `js/i18n/ru.js` — словарь строк (завести и начать наполнять из `03-content.md`).
**Критерии приёмки:**
- Логин через Google и через email magic link работает.
- «Вступить» открывает модалку логина, а не тост.
- После логина в `profiles` есть строка пользователя.
- Состояние логина видно в хедере (имя/выход).

## T2 — Чтение и витрина проектов
**Выход:**
- `js/projects.js` — `fetchPublishedProjects({ filter, sort, page })`,
  `fetchProjectById(id)`.
- Компонент карточки проекта (обложка, название, автор, теги, апвоуты, кол-во сообщений).
- Рендер ленты на главной (секция «Проекты сообщества») и полной витрины `projects.html`
  с фильтрами (теги, инструменты, `is_core`) и сортировкой (свежие / апвоуты).
**Критерии приёмки:**
- На главной и в `projects.html` показываются только `published`.
- Фильтры и сортировка работают.
- Пустое состояние показывает текст-заглушку (см. 03-content.md).
- **Безопасность:** любой пользовательский текст (название, автор, описание, теги, имена в
  комментах/списке участников) при вставке в DOM ЭКРАНИРУЕТСЯ. Не совать сырой `innerHTML`
  с данными из БД. Переиспользуй `escapeHtml` из `js/app.js` (вынеси в общий util при надобности),
  либо строй узлы через `textContent`/`createElement`. Это защита от хранимого XSS.

**Статус:** СДЕЛАНО (вместе с T-D3, принято на ревью 2026-07-02). Промпт —
`docs/prompts/T2-sonnet.md`.

## T2.1 — Обвязка `projects.html` + гигиена после T2
Мини-задача по итогам ревью T2. Промпт — `docs/prompts/T2.1-sonnet.md`.
**Выход:**
- `projects.html`: шапка как на `index.html` (nav + `data-header-auth`, подключить
  `js/app.js`), убрать Telegram-CTA из шапки; title/meta/OG под витрину сообщества.
- Токены вместо хардкода в новых секциях `styles.css` (`.community-*`, `.showcase-*`).
- `js/projects.js`: вторичная сортировка `created_at` при `sort='top'`.
- `.stylelintignore` (`dist/`), `lint:css:fix` для preexisting-ошибок.
**Критерии приёмки:**
- Авторизация работает в шапке `projects.html`; консоль чистая; регресса витрины нет.
- `npm run lint:js` и `npm run lint:css` — 0 ошибок.
**Статус:** СДЕЛАНО (принято на ревью 2026-07-02). На ревью дочинено: возвращён
`-webkit-background-clip: text` (автофикс stylelint снёс префикс — на Chrome <120
градиентный текст становился невидимым), правило `property-no-vendor-prefix` в
`.stylelintrc.json` теперь игнорирует `background-clip`.

## T3 — Добавление проекта
**Выход:**
- `submit.html` + `js/submit.js`.
- Гейт логина; форма (название, описание, ссылка, обложка → Storage `covers`, теги,
  инструменты); вставка `projects` со `status='pending'`.
- Сообщение об успехе + ссылка в Telegram.
**Критерии приёмки:**
- Незалогиненный видит гейт, не форму.
- Отправленный проект появляется в БД как `pending`, на витрине НЕ виден.
- Обложка загружается в Storage, URL сохраняется.
- Валидация обязательных полей и типов/размера картинки.

**Статус T3:** код сделан и отревьюен (2026-07-02, промпт `docs/prompts/T3-sonnet.md`).
На ревью дополнительно проверено из браузера: RLS отбивает анонимный insert
`published` (42501), pending снаружи невидим, upload в чужую папку `covers` — 403.
Осталась ручная приёмка Тёмой: реальный логин → отправка проекта → проект в БД
как `pending`, обложка в `covers/<uid>/`.

## T4 — Страница проекта и сообщения
**Выход:**
- `project.html?id=...` + `js/project.js`.
- Полная карточка + лента `comments` (`published`) + форма для залогиненных.
- Апвоут (insert/delete в `project_upvotes`, оптимистично).
**Критерии приёмки:**
- Сообщения читаются всеми, пишутся только залогиненными.
- Апвоут уникален на пользователя, счётчик обновляется.
- Несуществующий/неопубликованный id → аккуратная ошибка/404.

**Статус:** код сделан, промпт `docs/prompts/T4-sonnet.md`. Проверено из браузера на живых
данных: карточка (обложка/градиент, инструменты, теги, автор, ✦ для `is_core`, кнопка
«Открыть проект»), пустое «Обсуждение», гейт для анонима (комменты + апвоут открывают модалку
логина), `?id` отсутствует / мусорный / несуществующий → экран «не найден» с верным `<title>`,
мобильная вёрстка, консоль и сеть чистые, `lint:js`/`lint:css` — 0 ошибок. По пути найден и
исправлен баг: `.pd-hero`/`.pd-cover-img`/`.pd-cover-core`/`.comment-gate`/`.comment-form`/
кнопка «Открыть проект» использовали `display:flex`, что перебивало атрибут `hidden` —
добавлены явные `[hidden]{display:none}`. Также была коллизия имён классов со старыми
`.project-hero`/`.project-tags`/`.project-description` (дизайн-портфолио `projects/*.html`) —
новые классы страницы проекта сообщества переименованы в `.pd-*`.
Не проверено вживую (нужна подтверждённая почтой учётка, регистрация в Supabase требует
подтверждения письмом — так же было в T3): отправка коммента залогиненным, XSS-тест
`<img src=x onerror=alert(1)>` в теле коммента, реальный insert/delete в `project_upvotes` и
персист состояния апвоута после перезагрузки. Логика (textContent-рендер коммента, insert с
`author_id: auth.uid()`, RLS на `comments`/`project_upvotes`) проверена по коду и совпадает с
проверенным паттерном T2/T3.
**Ревью (2026-07-02): код принят.** Дополнительно проверено из браузера: RLS отбивает
анонимные insert в `comments` и `project_upvotes` (42501); клик апвоута анонимом открывает
модалку; экраны «не найден» и живая карточка — ок. Дочинено на ревью: ошибка отправки
коммента больше не глотается молча — добавлен `[data-comment-error]` + ключ
`project.comment.error`. Ручная приёмка Тёмой (логин → коммент → апвоут) — вместе с T3.

## T-D — Новый визуальный стиль (выполняется параллельно T1–T4, ДО T5)
**Контекст:** `06-design-direction.md`. Источник стиля — дизайн-система из Claude Design
(через `/design-sync`). Это не косметика, а смена сути визуала: уход от элегантной
«природной» эстетики с засечками к энергии кода/ИИ/созидания. Сайт должен сам выглядеть как
крутой навайбкоженный продукт.
**Выход:**
- `styles.css` (или новый `css/tokens.css`) — токены из дизайн-системы как CSS-переменные
  в `:root`: цвета, градиенты, типографика (дисплей + текст + моно), spacing, радиусы,
  тайминги анимаций.
- Подключение новых шрифтов (геометрический гротеск + читаемый сан-сериф + моно). Cormorant
  Garamond убрать.
- Базовые компоненты по `06`: кнопки (главный CTA «Вступить» — самый заметный), карточка
  проекта, чипы тегов/инструментов, поля форм, хедер/навигация, бейдж «флагман We Designerz»,
  блок сообщения, пустые состояния, тосты.
- Анимации на CSS/малом JS (без тяжёлых либ): hero, hover карточек, печать текста, переходы.
  Уважать `prefers-reduced-motion`.
**Критерии приёмки:**
- Старая эстетика (Cormorant, природно-студийный вид) полностью убрана.
- Контраст текста проходит WCAG AA.
- Анимации не тормозят, отключаются при `prefers-reduced-motion`.
- Стиль — единый источник правды через CSS-переменные; страницы T5/T6 строятся на нём.
- На hero возникает эффект «вау, это само навайбкожено».
- Статус первого прохода: базовые токены/шрифты/hero-терминал СДЕЛАНЫ. Осталась
  де-шаблонизация — см. T-D2.

## T-D2 — Де-шаблонизация (после первого прохода, ДО финала T5)
**Контекст:** раздел «Анти-шаблон» в `06-design-direction.md`. Режим — средняя
де-шаблонизация: скелет страниц сохраняем, убираем «типовой AI-лендинг».
**Выход:**
- Убрать кикер-под-копирку: `.section-label` не над каждым блоком, варьировать вход в секции
  (крупное число / одно слово / моно-фрагмент промпта / без кикера).
- Переделать блок «4 шага со стрелками» (`.how-strip`) — уйти от шаблонного вида.
- Сломать однообразный ритм: чередовать плотность (плотная лента ↔ крупное утверждение),
  ввести асимметрию.
- Сквозная вайб-атрибутика: моно-промпты, чрома терминала как мотив, ✦ как фирменный глиф,
  бейджи инструментов, тосты «X только что добавил проект».
- Концепт «живая генерация» как акцент: hero-терминал перетекает в витрину сообщества.
**Критерии приёмки:**
- Нет ощущения «шаблон, как у всех» — секции различаются по подаче и плотности.
- Вайб-артефакты присутствуют и читаются как «живое сообщество».
- Ничего не сломано на мобиле; `prefers-reduced-motion` уважается.

## T-D3 — Карточка проекта в стиле YouTube
**Контекст:** `06`, раздел «Компоненты». Заменяет текущую `.community-card`.
**Выход:**
- Обложка 16:9, заголовок в 2 строки, аватар + автор, мета-строка (лайки/просмотры/тег),
  бейдж инструмента в углу обложки. Используется в витрине главной и `projects.html`.
**Критерии приёмки:**
- Читается как привычная видео-карточка; сетка адаптивна; данные приходят из источника T2.
**Статус:** СДЕЛАНО (в связке с T2, принято на ревью 2026-07-02).

## T-D4 — Живая лента участников + профиль
**Контекст:** `06`. Заменяет захардкоженные фейковые `@ники` в блоке «уже с нами».
**Выход:**
- Самолистающийся ряд реальных участников (аватар + имя) из `profiles` (Supabase).
- Каждый кликабелен → страница/модалка профиля: чем занимается, его проекты.
- Зависит от T1 (данные профилей). До наполнения БД — аккуратное пустое/сид-состояние.
**Критерии приёмки:**
- Никаких выдуманных ников; лента реальная и кликабельная; ведёт на профиль.

## T-D5 — Единая навигация, убрать фейк и починить ссылки
**Выход:**
- **Единая шапка на всех страницах** (по фидбэку Тёмы 2026-07-02: с главной нельзя
  попасть на `projects.html`, две разные навигации): `Главная · Проекты (projects.html) ·
  О нас · [Вступить]`. Якорные пункты (`#showcase`, `#how`, `#community`) из шапки убрать —
  секции доступны скроллом. Одна и та же шапка: index, projects, submit, project, profile,
  about, 404.
- **«Все проекты →»** в шапке секции витрины на главной (`showcase-head`, см. `03-content.md`)
  → `projects.html`.
- Убрать/переформулировать оставшиеся выдуманные цифры (hero-цифры уже реальные, T-D2 —
  проверить, не осталось ли фейка в других местах).
- Telegram: убрать личную `t.me/androsovart` из community-кнопок на about/projects/404
  (на index уже сделано). Канал: `https://t.me/wedesignerz`; чат: `https://t.me/+98-s06KjbuNhM2Zi`.
  Развести «Telegram-канал» / «Чат сообщества». Hire-CTA на `projects/*.html` не трогать (T6).
- CTA «Вступить» (`[data-join]`) → открывает модалку логина/регистрации (см. T1), не тост —
  на страницах, где ещё тост/старое поведение.
**Критерии приёмки:**
- С любой страницы в один клик: на главную, на витрину `projects.html`, «Вступить».
- Шапка идентична на всех страницах; на проде нет выдуманных данных; TG-ссылки корректные.
**Статус:** СДЕЛАНО (принято на ревью 2026-07-03). Шапка едина на 7 страницах,
`androsovart` остался только в hire-CTA на `projects/*.html` (по плану, уберётся в T6),
CTA на about переведены на `data-join`, линтеры 0.

## T-UX1 — UX-логика по фидбэку живого прогона (после T-D4/T-D5)
Фидбэк Тёмы 2026-07-02 + UX-проход Opus. Промпт — `docs/prompts/T-UX1-sonnet.md`.
**Выход:**
- Auth-aware CTA: гостю «Вступить» (модалка), участнику «Добавить проект» (`submit.html`);
  имя в шапке — ссылка на свой профиль.
- `submit.html`: авто-`https://` в поле ссылки (blur/до валидации), хелпер
  `normalizeHttpUrl` в `js/util.js`.
- Обсуждение: форма над лентой, новые сверху, редактирование/удаление своих комментов
  (инлайн-подтверждение, без alert/confirm; RLS уже разрешает update/delete own).
- `project.html`: единая контентная колонка (равные max-width/отступы; замер ревью:
  left 11px vs 81px, разрыв ~300px), разрыв верх↔обсуждение ≤ ~80px, убрать пустоту
  hero при отсутствии обложки.
- Экран успеха submit: кнопка «Добавить ещё один».
**Критерии приёмки:** см. промпт (живое переключение CTA, авто-https без задвоения,
edit/delete только своих, выровненный layout, мобилка 375px, консоль чистая, линтеры 0).

## T5 — Главная: манифест + «Кто мы» (сужено 2026-07-03)
Бо́льшая часть исходного T5 закрыта редизайном (T-D…T-D5, T-UX1): hero, витрина,
how-terminal, финальный CTA и footer уже сообществоцентричные, студийный оффер убран,
навигация единая (принята в T-D5 — НЕ менять). Остаток — промпт `docs/prompts/T5-sonnet.md`:
**Выход:**
- Секция «Манифест» (тексты из `03-content.md`) между `#how` и `#community`.
- Секция «Кто мы»: тизер команды (фото из `assets/images/team/`), доказательство +
  «Смотреть кейсы →» → `about.html`, **единственный** hire-CTA → личка `t.me/androsovart`.
**Критерии приёмки:**
- Ровно один hire-CTA на главной, в зоне «Кто мы»; тарифов/«Начать проект» нет.
- Порядок секций: hero → showcase → how → манифест → кто мы → community → footer.
- Не шаблонно (см. `06`), мобилка 375px ок, линтеры 0.
**Статус:** СДЕЛАНО (принято на ревью 2026-07-03). На ревью дочинено: hire-CTA
(`white-space: nowrap` у `.btn-secondary`) давал горизонтальный скролл на 375px —
добавлен перенос в мобильном брейкпоинте. Тексты ролей команды — черновик
(`03-content.md`), финальные формулировки за Тёмой.

## T6 — «Кто мы» + перенос дизайн-портфолио
**Выход:**
- `about.html` переработать: история, команда (по реальному весу), дизайн-портфолио как
  доказательство, полный hire-CTA.
- Существующие `projects/*.html` оставить, перелинковать из about как кейсы.
**Критерии приёмки:**
- Дизайн-кейсы доступны и подписаны как доказательство экспертизы.
- Hire-CTA ведёт в Telegram-личку.
**Статус:** СДЕЛАНО (принято на ревью 2026-07-03, промпт `docs/prompts/T6-sonnet.md`).
Все 8 кейсов перелинкованы (были сиротами), hire-зона с 2 отзывами → личка,
финал — data-join. Мёртвый студийный CSS снесён. Тексты hero-истории и лида
портфолио — черновик, финальные формулировки за Тёмой.

## T7 — Админ-модерация
**Выход:**
- `admin.html` + `js/admin.js`. Виден только роли `admin`.
- Список `pending` проектов, кнопки «Опубликовать» / «Отклонить», тумблер `is_core`,
  скрытие комментариев.
**Критерии приёмки:**
- Не-админ не видит страницу/данные (проверяется и UI, и RLS).
- Публикация переводит проект в витрину.

**Статус:** код сделан, промпт `docs/prompts/T7-sonnet.md`. Гейт: гость/не-админ видит
честный экран «Доступа нет» (проверено из браузера — запрос `profiles.role` не уходит,
пока нет сессии), `noindex,nofollow`, смена auth-состояния (logout) сбрасывает в «Доступа
нет». Три вкладки (на модерации/опубликованные/отклонённые) + вкладка «Комментарии»
(последние 50 `published`), живой переезд карточек между вкладками и счётчики без
перезагрузки, инлайн-подтверждение (без alert/confirm) для «Отклонить» и «Скрыть»,
тумблер `is_core`. По пути найден и исправлен тот же баг, что в T4: `.btn-secondary`/
`.btn-primary` с `display:inline-flex` перебивали атрибут `hidden` у кнопки-триггера в
инлайн-подтверждении — добавлено `.adm-confirm-wrap > button[hidden]{display:none}`.
Проверено в браузере (собранные вручную тестовые карточки, т.к. под реальным admin
нужен ручной шаг ниже): рендер карточек/чипов/тумблера/подтверждений, мобилка 375px без
горизонтального скролла, консоль чистая, линтеры 0. **Не проверено вживую** (нужна
роль `admin`, назначается только Тёмой через SQL, см. промпт): реальная публикация
pending → появление в витрине, RLS-ответ на попытку не-админа (участника) открыть
`admin.html`.
**Ревью (2026-07-03): код принят.** Проверено на ревью: XSS-гигиена (все данные через
`textContent`/`createElement`), все `admin.*` i18n-ключи есть в словаре, `admin.html`
нет в `sitemap.xml`, гость на живой странице — ноль REST-запросов и экран «Доступа нет».
Дочинено на ревью: после ошибки RLS в инлайн-подтверждении кнопки «Да/Отмена» оставались
заблокированными навсегда — теперь подтверждение сворачивается и триггер возвращается.
Ручная приёмка Тёмой (роль admin → публикация pending → витрина; участник → «Доступа
нет») — остаётся.

## T8 — SEO, мета, чистка
**Выход:**
- Обновить title/description/keywords/OG на всех страницах (см. 03-content.md).
- Новая OG-картинка под позиционирование.
- Обновить `sitemap.xml`, `robots.txt` под новые страницы.
- Удалить мёртвые блоки старого студийного оффера (тарифы и пр.), прогнать линтеры
  (`npm run lint:js`, `npm run lint:css`).
**Критерии приёмки:**
- Линтеры зелёные.
- Нет битых ссылок и осиротевших ассетов.
- OG-превью корректное при шеринге.
**Статус:** СДЕЛАНО (принято на ревью 2026-07-03, промпт `docs/prompts/T8-sonnet.md`).
Проверено на ревью: удалённые CSS-классы автоматически сверены с html/js (конфликтов
нет), битых ссылок в рабочих страницах 0 (все «битые» — внутри локального `dist/`,
он в .gitignore и не деплоится). Дочинено на ревью: шапка `projects/*.html` была
сломана ещё с редизайна (img-лого без CSS) — приведена к единой разметке T-D5 с
текстовым лого, hire-CTA сохранён.
**Осиротевшие ассеты (решение за Тёмой, не удалены):** `assets/images/carousel/*` (17),
`assets/logos/clients/*.svg` (9), `assets/images/textures/*.jpg` (4),
`assets/icons/Mark-White-on-Black_1024.png` + `_256.png`, `assets/images/clients/Maxim.jpeg`,
`assets/images/projects/eazyenergy/eazyenergy3.jpeg`,
`assets/images/projects/emtechinvest/imtechinvest2.jpeg`,
`assets/logos/nenasheva-uma-delo.svg`, `assets/logos/WDLogo.svg` + `WDLogoMobile.svg`
(стали сиротами после унификации шапки кейсов), `oblikflow.html` + `assets/oblikflow/`
(возможно, живёт у клиента по прямой ссылке).
**Руками за Тёмой:** новая OG-картинка `assets/images/og-image.jpg` (1200×630) под
сообщество; решение по списку сирот выше. К запуску учесть: workflow деплоит весь корень —
`docs/`, `CLAUDE.md`, `supabase/schema.sql` публично доступны на проде (не секреты,
но внутренняя кухня; при желании — вынести из деплоя или смириться).

## T9 (после запуска) — Профиль участника
**Выход:** `me.html` — редактирование профиля, список своих проектов и их статусов.
Не блокирует запуск.

---

## Порядок и зависимости
T0 → T1 → (T2, T3 параллельно после T1) → T4. Дизайн-ветка T-D → T-D2 (де-шаблон) →
T-D3 (YouTube-карточки) идёт параллельно T1–T4 и готова ДО финала T5. T-D4 (лента участников)
зависит от T1. T-D5 (убрать фейк + TG + логин-модалка) — быстрый, после T1. Затем
T-UX1 (UX-фиксы, после T-D4/T-D5) → T5 → T6 → T7 → T8 → запуск → T9.
Минимум для публичного анонса: **T0–T8 (включая T-D…T-D5)** + сидинг из `05-launch.md`.
