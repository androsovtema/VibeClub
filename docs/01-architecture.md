# 01 — Архитектура и данные

## Стек

- **Фронт:** оставляем статический HTML/CSS/JS, без фреймворка и сборки.
  **Production с 2026-07-18** — Caddy на RU-VPS, домен `wedesignerz.com`;
  `.github/workflows/deploy-vps.yml` по push в `main` проверяет и собирает
  `_site/` только из файлов сайта: `docs/`, `audits/`, `supabase/`, `CLAUDE.md` и
  конфиги на публичный сайт **не попадают** (правило от 2026-07-06 — до этого весь
  репозиторий, включая внутренние доки, был публично доступен по прямым URL).
  Releases активируются атомарным symlink; rollback воспроизведён. GitHub
  остаётся репозиторием, а Pages временно сохраняет тот же release только как
  DNS rollback-origin на 72-часовое окно наблюдения, не как авторитетный origin.
- **Бэкенд:** self-hosted Supabase на RU-VPS: Postgres + GoTrue + PostgREST +
  Storage за `https://api.wedesignerz.com`. T-CUTOVER закрыт 2026-07-15;
  старый cloud-проект больше не production и имеет выключенные регистрации.
  Прямой rollback на него запрещён. Клиент `@supabase/supabase-js` вендорен
  локально в `js/vendor/`.
- **Почта:** локальный `mail-bridge` передаёт транзакционные письма Auth в
  Unisender Go. **Аналитика:** self-hosted Umami на
  `https://stats.wedesignerz.com`.
- **Чат сообщества:** внешний Telegram-чат — дополнительный канал общения.
  «Вступить» открывает auth/членство на сайте, не ведёт прямо в Telegram.

Почему так: рабочую статику, API-контракт и визуал не переписываем; меняются
только origin фронта и размещение совместимого бэкенда. Это снижает миграционный
риск и сохраняет существующий клиент/RLS.

## Ключевой принцип безопасности: anon key публичный → защита через RLS

В статическом сайте `anon`-ключ Supabase виден в исходниках браузера. Это нормально и
ожидаемо — но значит, что **вся защита данных строится на Row Level Security (RLS)** в
Postgres, а не на «спрятанном» ключе. Каждая таблица обязана иметь включённый RLS и явные
политики. Без этого любой сможет писать в БД напрямую. Это самый важный пункт всей реализации.

`service_role`-ключ **никогда** не попадает на фронт. Он только для серверных задач
(если появятся — например, Edge Functions для модерации).

## Авторизация

Аудитория в основном РФ. Иностранный OAuth не используем: он не нужен для MVP
и создаёт юридический/инфраструктурный риск. Минимизируем барьер без него:

**MVP:**
- **Почта + пароль** — основной способ. Работает везде, без внешних сервисов.
- **Email magic link** — беспарольный запасной.

**Бэклог (после запуска):**
- **Telegram-логин** — сильный кандидат: сообщество живёт в Telegram, вход одним тапом.
  Не нативный провайдер Supabase — нужна серверная проверка подписи и отдельный
  юридический/security-review.
- **Телефон + SMS-код** — требует подходящего российского SMS-провайдера и
  отдельной оценки обработки телефона.

Google/GitHub/Apple OAuth не добавляем. Email пользователя при этом может быть
на любом почтовом домене.

Добавить проект и писать сообщения можно только залогинившись. Просмотр — без логина.

## Схема данных (Postgres)

### `profiles`
Расширение `auth.users`. Создаётся триггером при регистрации.

| поле | тип | примечание |
|---|---|---|
| `id` | uuid PK | = `auth.users.id` |
| `display_name` | text | имя в сообществе |
| `avatar_url` | text | аватар (Storage или из OAuth) |
| `bio` | text | короткое о себе |
| `telegram` | text | ник в TG (опц.) |
| `website` | text | ссылка (опц.) |
| `github` | text | GitHub-ник (опц.) |
| `phone` | text | публичный телефон (опц.) |
| `email_public` | text | отдельная публичная почта, не login email |
| `custom_link_label`, `custom_link_url` | text | своя публичная ссылка (опц.) |
| `role` | text | `member` \| `core` \| `admin`, дефолт `member` |
| `skills` | text[] | навыки, до 12 значений |
| `open_to` | text[] | `collab` \| `orders` \| `team` |
| `created_at` | timestamptz | |

До закрытия T-CONSENT контактные поля остаются публичными по модели T23.
T-CONSENT добавит отдельный приватный журнал и database-level запрет сохранять
контакты без активного согласия на распространение.

### `projects`
| поле | тип | примечание |
|---|---|---|
| `id` | uuid PK | |
| `author_id` | uuid FK → profiles.id | |
| `title` | text | |
| `description` | text | что это и как навайбкожено |
| `cover_url` | text | обложка (Storage) |
| `images` | text[] | галерея, до 9 изображений |
| `project_url` | text | ссылка на живой проект |
| `tags` | text[] | теги/категории |
| `tools` | text[] | какими ИИ-инструментами сделано |
| `stage` | text | idea/prototype/mvp/users/commercial (опц.) |
| `looking_for` | text[] | feedback/testers/роль/партнёр/клиент/инвестор |
| `status` | text | `pending` \| `published` \| `rejected`, дефолт `pending` |
| `is_core` | bool | true = флагман самих We Designerz |
| `upvotes` | int | денормализованный счётчик (обновляется триггером) |
| `created_at` | timestamptz | |

### `comments` (сообщения под проектом)
| поле | тип | примечание |
|---|---|---|
| `id` | uuid PK | |
| `project_id` | uuid FK → projects.id | |
| `author_id` | uuid FK → profiles.id | |
| `body` | text | |
| `kind` | text | ux/idea/bug/market/contact/collab (опц.) |
| `status` | text | `published` \| `hidden`, дефолт `published` |
| `created_at` | timestamptz | |

### `project_upvotes` (уникальные лайки)
| поле | тип | примечание |
|---|---|---|
| `project_id` | uuid FK | PK составной |
| `user_id` | uuid FK | PK составной |
| `created_at` | timestamptz | |

Триггер пересчитывает `projects.upvotes` при insert/delete.

### `feedback`

Приватные обращения из модалки «Нашли проблему?». Создавать может только
авторизованный пользователь со своим `user_id`; список читает и обновляет
только админ.

| поле | тип | примечание |
|---|---|---|
| `id` | uuid PK | |
| `user_id` | uuid FK → auth.users.id | при удалении аккаунта становится null |
| `page` | text | страница отправки |
| `message` | text | сообщение 10–2000 символов |
| `contact` | text | необязательный контакт |
| `status` | text | `new` \| `done` |
| `created_at` | timestamptz | |

## Политики RLS (по таблицам)

**`profiles`**
- SELECT: всем (публичные профили).
- INSERT/UPDATE: только владелец (`auth.uid() = id`).

**`projects`**
- SELECT: всем, где `status = 'published'`. Автор видит свои в любом статусе. Админ видит все.
- INSERT: аутентифицированным, только `author_id = auth.uid()` и `status = 'pending'`
  (нельзя самопубликоваться).
- UPDATE статуса (`published`/`rejected`) и `is_core`: только `role = 'admin'`.
- UPDATE контента своей записи (пока `pending`): автор.
- DELETE: автор своей записи или админ.

**`comments`**
- SELECT: всем, где `status = 'published'` и родительский проект опубликован.
- INSERT: аутентифицированным, `author_id = auth.uid()`.
- UPDATE/DELETE: автор своей записи или админ (скрытие = `status='hidden'`).

**`project_upvotes`**
- SELECT: всем. INSERT/DELETE: только `user_id = auth.uid()`.

**`feedback`**
- INSERT: только `authenticated`, причём `user_id = auth.uid()`.
- SELECT/UPDATE: только админ. Публичного списка нет.

## Модерация и анти-спам

- Проекты публикуются после ручного одобрения админом (`status pending → published`).
  Простая админ-вьюшка — отдельная страница `/admin`, видна только роли `admin`.
- Комментарии автопубликуются, но серверный триггер ограничивает их: не чаще
  одного за 20 секунд и не более 30 в час на автора. Автор может редактировать и
  удалить свой комментарий; админ — скрыть.
- Turnstile принуждается GoTrue для Auth-операций. Пароль нового аккаунта —
  минимум 12 символов. Отдельной CAPTCHA на сабмите проекта нет.
- Storage bucket `covers`: публичное чтение объектов без публичного листинга,
  загрузка только авторизованным; лимит 10 МБ, типы jpeg/png/webp.

## i18n-готовность (EN позже, не сейчас)

Не строим английскую версию на запуск, но не закрываем дверь:
- Все UI-тексты держим в одном месте — `js/i18n/ru.js` (объект-словарь `{ key: text }`),
  а не разбросаны по HTML вперемешку. Позже добавляется `en.js` и переключатель.
- Пользовательский контент (проекты/комменты) остаётся на языке автора — не переводим.
- URL-структура закладывается под префикс языка на будущее (`/`, позже `/en/`), но сейчас
  без префикса.

Цель этапа MVP — чтобы добавление EN потом было «добавить файл словаря + переключатель»,
а не «переписать весь сайт».

## Конфиги и секреты

- Публичные `SUPABASE_URL` и `SUPABASE_ANON_KEY` лежат в `js/config.js` (это нормально,
  они и так публичны — защита в RLS).
- Никаких `service_role` ключей в репозитории.
- `.env` не нужен для статики. Серверные секреты self-host Supabase, Turnstile,
  Unisender и S3 лежат только в `/root/vibeclub/.env` на VPS и не коммитятся.
- Живую схему меняют только идемпотентными файлами
  `supabase/migrations/*.sql`; `supabase/schema.sql` синхронизируется как канон
  чистой установки, но целиком поверх production не накатывается.
