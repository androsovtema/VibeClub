# 08 — Как мы работаем (принцип и роли)

Этот файл — точка входа для любой новой сессии. Если ты (или новая модель) открыл проект
и не знаешь контекста — читай отсюда, потом `docs/README.md`.

## Роли

**Тёма — владелец продукта и вайбкодер.**
Держит видение, принимает решения, придумывает и направляет. Пишет на естественном языке
по-русски, не обязан знать код. Смотрит результат в браузере и говорит, что не так.
Делает то, что требует его личности/кредов (аккаунты, ключи, деплой, Telegram, видео).

**Opus 4.8 (я) — «мозг» проекта.**
Не пишу продуктовый код (кроме мелких точечных правок). Моя работа:
- стратегия и позиционирование, архитектура, схема БД и безопасность (RLS);
- вся документация в `docs/`;
- разбиваю работу на задачи для Sonnet с критериями приёмки;
- ревью результата, диагностика, план дальнейших шагов;
- задаю уточняющие вопросы, когда решение за Тёмой; иначе беру разумный дефолт и иду.
Общаюсь с Тёмой по-русски, коротко и по делу, без воды.

**Sonnet — исполнитель.**
Пишет код строго по задачам из `docs/04-tasks-sonnet.md`. Одна задача за раз, до критериев
приёмки. Не меняет архитектуру и позиционирование — они заданы в `docs/`.

## Как течёт работа (цикл)

```
Тёма (идея / правка на русском)
   → Opus: обсуждает, решает, обновляет docs/, пишет задачу + детальный промпт
   → Тёма: копирует промпт в сессию Sonnet (или переключает модель на Sonnet)
   → Sonnet: пишет код по задаче
   → Тёма/Opus: смотрят в браузере (localhost:8080), ревью
   → правки → повтор, пока не примем
```

Opus и Sonnet **не общаются напрямую**. Интерфейс между ними — документация в `docs/` и
готовый промпт, который Тёма переносит. Поэтому задачи пишутся самодостаточно: с путями,
контекстом и критериями приёмки, чтобы Sonnet не нужен был этот чат.

## Где что лежит

- `CLAUDE.md` — правила проекта (язык, стек, токены). Читается автоматически каждой сессией.
- `docs/README.md` — оглавление документации.
- `docs/00-vision.md` … `07-supabase-setup.md` — видение, архитектура, ИА, контент, задачи,
  запуск, дизайн, настройка Supabase.
- `docs/04-tasks-sonnet.md` — **список задач для Sonnet** (T0…T9, T-D…T-D5) с приёмкой.
- `docs/08-workflow.md` — этот файл.
- `supabase/schema.sql` — схема БД + RLS (применяется в Supabase SQL Editor).
- `js/config.js` — публичные ключи Supabase (защита на RLS, service_role сюда НЕ кладём).
- Память ассистента (вне репо) — устойчивые факты о проекте и предпочтениях Тёмы.

## Запуск и деплой (обновлено 2026-07-07 — проект переехал в отдельный репо)

Проект вынесен из `wdcom` (студия дизайна) в самостоятельный репозиторий/папку.
Старая модель «в тени на ветке `redesign`» больше НЕ действует.
- Репо: `androsovtema/VibeClub`, рабочая ветка — **`main`**. Папка на ПК:
  `/Users/prosto/Desktop/01_Work/Products/VibeClub`.
- Локально: `python3 -m http.server 8080` → `http://localhost:8080/index.html`.
  Не открывать `file://` и не `npm run dev` (скрипта нет).
- Деплой: GitHub Pages через **Actions** (`.github/workflows/deploy.yml`), по push
  в `main`. URL превью: `https://androsovtema.github.io/VibeClub/`. Внутреннее
  (`docs/ supabase/ CLAUDE.md`/конфиги) в артефакт не попадает — не публикуется.
- **Staging, закрыт от индексации:** `robots.txt` = `Disallow: /`, `CNAME` убран
  (домен `wedesignerz.com` остаётся за студией `wdcom`). До публичного запуска
  живых людей пускаем только по прямой ссылке.
- **Название временное** («VibeClub»); финальный бренд не выбран — ребренд
  (лого/meta/домен/`docs/`) отдельной задачей.
- Supabase — **отдельный** проект `ndhyvspgkelxgqmfmmry` (не общий со студией),
  публичные ключи в `js/config.js`, схема из `supabase/schema.sql`.
- `wdcom` (старый репо) заморожен на `main` = студия; работу там не ведём.

## Принципы

- **Русский язык** во всём общении (см. `CLAUDE.md`).
- **Честность данных:** никаких выдуманных цифр/участников на проде. Только реальное из БД
  или честная формулировка. (См. `00-vision.md`.)
- **Не шаблонно:** сайт не должен выглядеть как типовой AI-лендинг (см. `06`, раздел
  «Анти-шаблон»).
- **Безопасность:** anon-ключ публичен, вся защита на RLS. `service_role` никогда на фронте.

## Как пользоваться этим в новой сессии

1. Прочитать `CLAUDE.md` (авто) → `docs/08-workflow.md` (этот) → `docs/README.md`.
2. Посмотреть текущий статус и «Порядок и зависимости» в `docs/04-tasks-sonnet.md`.
3. Продолжать с ближайшей незакрытой задачи.

## Текущий статус (обновлять по ходу)

- Позиционирование, архитектура, контент, дизайн-направление — задокументированы.
- Визуал: базовый стиль «Электрический холст» сделан; осталась де-шаблонизация (T-D2…T-D5).
- Supabase: проект создан, `schema.sql` применён, Email-авторизация включена, `config.js`
  прописан.
- **T1 сделана** (клиент + email-авторизация + модалка логина, коммит `cb3754d`).
- **T2 + T-D3 сделаны и приняты** (2026-07-02): реальная витрина из Supabase на главной и
  `projects.html`, YouTube-карточки, фильтры/сортировка. В БД — временные тестовые проекты
  (перед запуском заменить сидингом из `05-launch.md`).
- **T2.1 сделана и принята** (2026-07-02): `projects.html` — шапка/auth/meta как на главной,
  токены вместо хардкода, линтеры 0 ошибок (`.stylelintignore` для `dist/`).
- **T3 сделана, код принят** (2026-07-02): `submit.html` + `js/submit.js` — гейт логина,
  форма, upload обложки в `covers/<uid>/`, insert `pending`. RLS-защита проверена из
  браузера. Осталась ручная приёмка Тёмой (реальный логин + отправка).
- **T4 сделана, код принят** (2026-07-02): `project.html` + `js/project.js` — карточка,
  обсуждение, оптимистичный апвоут, экран «не найден». RLS на `comments`/`project_upvotes`
  проверена из браузера. Ручная приёмка Тёмой (логин → сабмит → коммент → апвоут) — общая
  для T3+T4.
- **T-D2 сделана** (2026-07-02): главная де-шаблонизирована — честные live-цифры хиро из
  Supabase (`js/stats.js`, блок скрывается при ошибке сети), кикер витрины заменён на
  терминальную строку промпта, `.how-strip` («4 шага со стрелками») переделан в
  `.how-terminal` (чрома терминала, крупные моно-номера шагов слева, асимметрия), ритм
  секций разбит (плотная витрина ↔ терминал ↔ разреженный CTA-манифест). Линтеры чисто,
  проверено в браузере на 375px и десктопе. Принято на ревью (2026-07-02): live-цифры,
  терминал-блок, якоря, консоль — проверены живьём. Ручная приёмка Тёмой — не сделана.
- **Ручная приёмка T3+T4 Тёмой пройдена** (2026-07-02): логин (учётка через Dashboard,
  Auto Confirm), коммент и апвоут работают живьём. Из прогона — пакет UX-фидбэка,
  оформлен в задачу **T-UX1** (`docs/prompts/T-UX1-sonnet.md`): auth-aware CTA,
  авто-https в форме, форма коммента над лентой + edit/delete своих, layout project.html.
- **T-D4 сделана и принята** (2026-07-02): живая лента участников (реальные profiles,
  marquee, без фейка), `profile.html` + `js/profile.js`, тост «недавно в витрине».
  Проверено на ревью живьём: чипы/профиль/«не найден»/тост с реальными данными, RLS ок.
- **T7 сделана** (2026-07-03): `admin.html` + `js/admin.js` — гейт по роли `admin`
  (честное «Доступа нет» для гостя/участника, `noindex,nofollow`), вкладки
  На модерации/Опубликованные/Отклонённые/Комментарии со счётчиками, публикация/отклонение/
  возврат/снятие с публикации, тумблер `is_core`, скрытие комментария — всё с живым
  переездом карточек и инлайн-подтверждением (без alert/confirm). Линтеры 0, мобилка ок.
  Ручная приёмка Тёмой не сделана: нужно назначить себе роль `admin` в Supabase SQL Editor
  (см. промпт `docs/prompts/T7-sonnet.md`), затем проверить публикацию pending-проекта.
- **T8 сделана и принята** (2026-07-03): SEO-мета/OG, sitemap/robots, чистка мёртвого CSS,
  единая шапка на кейсах `projects/*.html`. Руками за Тёмой: OG-картинка, решение по
  списку сирот (см. `04-tasks-sonnet.md`, T8).
- **Продуктовый аудит принят как новый пул** (2026-07-03):
  `audits/product-cjm-2026-07-03/README.md` → план в `docs/09-growth-plan.md`,
  задачи **T10–T14** в `04-tasks-sonnet.md`. Минимум до анонса теперь:
  T0–T8 + **T10 + T11** + сидинг. Под P1 подготовлена миграция БД
  `supabase/migrations/2026-07-03-stage-ask-profile.sql` (применяет Тёма перед T12).
- **T10 сделана и принята** (2026-07-03): развод CTA «профиль/чат» (hero-note,
  community-note), подзаголовок auth-модалки, submit-гейт «что/как/зачем».
  Проверено живьём (desktop + 375px), линтеры 0. Тексты нот — черновик за Тёмой.
- **Найдено на ревью T10: T-UX1 не была выполнена** — существовал только
  docs-коммит с промптом (`b3bfb3c`), кода нет (нет auth-aware `data-join`,
  `normalizeHttpUrl`, edit/delete комментов, layout project.html). Возвращена
  в очередь, включена в минимум до анонса.
- **T11 сделана и принята** (2026-07-03, `fd3a249`): hero и фильтры `projects.html`
  сжаты, сетка видна на 545px (десктоп) / 625px (мобилка) — замеры подтверждены
  на ревью живьём.
- **T-UX1 сделана и принята** (2026-07-03): auth-aware CTA, имя в шапке →
  профиль, авто-https, форма комментов над лентой + edit/delete своих,
  layout project.html выровнен. Дочинен на ревью повторный баг
  `display:flex` vs `[hidden]` (4-й случай — паттерн задокументирован в `04`).
  Ручная приёмка Тёмой: сценарий участника (CTA «Добавить проект»,
  правка/удаление своего коммента).
- **Ручная приёмка T-UX1 Тёмой пройдена** (2026-07-03): комменты (включая
  XSS-тест `<img onerror>` — рендерится текстом), edit/delete, сортировка,
  карточка — живьём. Из прогона — пакет UI-фидбэка, оформлен в **T-UX2**
  (`docs/prompts/T-UX2-sonnet.md`): стили кнопок комментов (браузерный дефолт —
  нет сброса `button`), кликабельные бейджи инструментов → `projects.html?tool=X`,
  селект сортировки → сегмент-контрол, иерархия «один primary на экран».
- **T-UX2 сделана и принята** (2026-07-04): стили кнопок комментов,
  кликабельные бейджи инструментов → `projects.html?tool=X`, сегмент-контрол
  сортировки, иерархия «один primary». Ручная приёмка Тёмой пройдена —
  новый пакет фидбэка оформлен в **T-UX3** (`docs/prompts/T-UX3-sonnet.md`):
  чип категории ≠ кнопка (+кликабелен `?cat=`), авторы кликабельны → профиль,
  обложка обязательна + стилизованный file input, анимация печати кикеров,
  роли how-terminal заметнее.
- **T-UX3 сделана и принята** (2026-07-04): чип категории — кликабельные
  метаданные (`?cat=`), авторы → профиль (шапка проекта + комменты), обложка
  обязательна + стилизованный выбор файла, печать кикеров при доскролле
  (`js/kickers.js`), роли how-terminal заметнее.
- **T-UX4 сделана и принята** (2026-07-04): фото в полной насыщенности,
  живая сетка участников (2–3 встречных ряда со смещением), полоска шапки
  узаконена как `.header::after` + течёт при hover (`:has`), скелетоны
  с shimmer на project/profile/витринах.
- **T15 сделана и принята** (2026-07-05): членство и доверие — копирайт «Вступить»,
  welcome-состояние, порог счётчиков, манифест, гейты «Вступить или войти».
- **T-SEC1 закрыта** (2026-07-06): эскалация привилегий в RLS отбита
  (`protect_privileged_columns`), security-check зелёный (`scripts/`, ранбук —
  `docs/12-runbook.md`).
- **T-EDIT сделана и принята** (2026-07-06): редактирование своих проектов в любом
  статусе (route A), RLS-политика `projects_update_own`.
- **Переезд в отдельный репо** (2026-07-07): `androsovtema/VibeClub`, ветка `main`,
  свой Supabase-проект `ndhyvspgkelxgqmfmmry`, staging закрыт от индексации.
  Детали — раздел «Запуск и деплой» выше.
- **T-UX5 сделана** (2026-07-07): прижатый футер (flex-колонка body).
- **P1-пул сделан** (2026-07-08): **T12** (стадия проекта + «что ищу», фильтр «ищут»),
  **T13** (категории комментов — направленный фидбек), **T14** (кабинет `me.html`,
  skills/open_to, поглотила T9), **T-UX6** (единая ширина контента,
  `--content-col: 860px`). Миграция 2026-07-03 не понадобилась — колонки уже
  в `schema.sql` чистой базы.
- **Дизайн-система задокументирована** (2026-07-08): `docs/13-design-system.md` —
  ритм страниц, контейнеры, токены `page-gutter`/`section-gap`.
- **T-D6 + T17 + T17.1 сделаны** (2026-07-08): живое hero-превью, галерея
  изображений проекта (до 9, `js/image.js` — optimizeImage/EXIF), надёжная
  загрузка галереи.
- **Кодовая очередь пуста. Блокер анонса — сидинг** (`05-launch.md`, руками Тёмы).
  Также за Тёмой: OG-картинка 1200×630 (T8), тексты (ноты T10, роли T5),
  приёмка T7 (назначить себе роль `admin`), e2e submit с обязательной обложкой.
- Следующая кодовая задача — **T16** (грейды v0/v1, промпт готов). Запуск только
  по команде Тёмы, на живых данных после анонса. Дальше — P2-бэклог
  (`09-growth-plan.md`).
