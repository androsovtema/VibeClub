# Промпт для Sonnet — задача T8 (SEO, мета, чистка)

> Скопируй всё под чертой в сессию Sonnet. Работать на ветке `redesign`
> (`git branch --show-current` должно = `redesign`). Отвечай по-русски.
> Последняя кодовая задача перед запуском (дальше — сидинг по `05-launch.md`).

---

Проект We Designerz — статический сайт-сообщество вайбкодеров, без сборки, GitHub Pages,
домен `wedesignerz.com`. Прочитай перед стартом: `CLAUDE.md`, `docs/08-workflow.md`,
`docs/03-content.md` (раздел «SEO / мета»), `docs/04-tasks-sonnet.md` (задача **T8**).

Это задача-финиш: мета, sitemap/robots, охота на битые ссылки и осиротевшие ассеты.
Ничего нового не строим — вычищаем.

## A. Мета и OG — аудит всех страниц

Эталон — `index.html` (там уже сделано) и `docs/03-content.md`:
- Пройди `projects.html`, `project.html`, `profile.html`, `submit.html`, `about.html`,
  `404.html`, `admin.html`: title / description / keywords / OG / Twitter / canonical —
  под новое позиционирование, без студийных формулировок. У каждой страницы —
  свой осмысленный description (не копия главной).
- Динамические страницы (`project.html`, `profile.html`): статичная мета — честная
  общая («Проект сообщества We Designerz»), JS уже подменяет title — не ломай.
- `404.html` — `<meta name="robots" content="noindex">`; OG не нужен.
- `admin.html` — noindex уже есть, проверь и не добавляй его никуда больше.
- OG-image: разметка на всех страницах должна указывать на `./assets/images/og-image.jpg`
  **абсолютным URL** (`https://wedesignerz.com/assets/images/og-image.jpg` — соцсети
  не понимают относительные пути). Саму картинку обновит Тёма (ручной шаг ниже) —
  файл и имя не меняй.

## B. sitemap.xml и robots.txt

- `sitemap.xml`: обновить `lastmod` (сегодняшняя дата) у всех; добавить `submit.html`;
  `admin.html`, `profile.html`, `project.html`, `404.html` НЕ добавлять (динамика/сервис).
  Дизайн-кейсы `projects/*.html` оставить.
- `robots.txt`: убрать мёртвый `Disallow: /project-template.html` (файла нет);
  добавить `Disallow: /admin.html`.

## C. Битые ссылки и осиротевшие ассеты

- Прогони все `href`/`src` во всех `*.html` (включая `projects/*.html`) на существование
  файлов. Почини найденное или доложи, если решение не очевидно.
- Осиротевшие ассеты: найди файлы в `assets/`, на которые никто не ссылается
  (сравни список файлов с грепом по html/css/js). **Кандидаты в отчёт, НЕ удалять
  без явного списка** — сначала покажи список Тёме в отчёте. Известные кандидаты:
  `assets/logos/WDLogo.svg`, `assets/logos/WDLogoMobile.svg` (логотип теперь текстовый),
  `assets/logos/nenasheva-uma-delo.svg`.
- **`oblikflow.html` + `assets/oblikflow/` НЕ трогать** — страница-сирота, но возможно
  живёт по прямой ссылке у клиента. Решение за Тёмой; просто упомяни в отчёте.
- Мёртвый CSS: быстрый проход по `styles.css` на классы, которых больше нет ни в одном
  html/js (аккуратно: классы, создаваемые из JS — `adm-*`, `pd-*`, `pf-*`, `member-*`,
  `comment-*`, `auth-*` и т.п. — проверяй и по js). Сноси только очевидное.

## D. Финальный прогон

- `npm run lint:js` / `npm run lint:css` — 0 ошибок.
- Пройди все страницы локальным сервером: консоль чистая, ничего не разъехалось.
- Мобилка 375px на изменённых страницах.

## Ограничения

- Логику/дизайн не менять — только мета, sitemap/robots, чистка.
- `js/i18n/ru.js`, токены — как везде. Секретов в коде нет и не появится.

## Проверка (критерии приёмки)

- Все страницы: осмысленные title/description, canonical, OG с абсолютным URL картинки.
- `sitemap.xml` актуален (+submit, без admin/404/динамики), `robots.txt` без мёртвых
  правил, admin закрыт.
- Нет битых `href`/`src`; список осиротевших ассетов — в отчёте (без удаления).
- Линтеры 0, консоль чистая.

## Ручной шаг для Тёмы

Новая OG-картинка `assets/images/og-image.jpg` (1200×630) под сообщество вайбкодеров —
текущая от студийной эпохи (апрель). Плюс решение по `oblikflow.html` и списку
осиротевших ассетов из отчёта Sonnet.

## После выполнения

Отчитайся кратко: файлы, список осиротевших ассетов, что осталось руками.
Дальше — сидинг и чек-лист запуска (`docs/05-launch.md`), затем merge `redesign` → `main`
(только по команде Тёмы — это деплой).
