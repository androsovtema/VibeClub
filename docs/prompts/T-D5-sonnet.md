# Промпт для Sonnet — задача T-D5 (единая навигация + чистка ссылок и фейка)

> Скопируй всё под чертой в сессию Sonnet. Работать на ветке `redesign`
> (`git branch --show-current` должно = `redesign`). Отвечай по-русски.
> Выполнять ПОСЛЕ T-D4 (профиль `profile.html` уже должен существовать).

---

Проект We Designerz — статический сайт-сообщество вайбкодеров, без сборки, GitHub Pages,
бэкенд Supabase. Прочитай перед стартом: `CLAUDE.md`, `docs/08-workflow.md`,
`docs/02-information-architecture.md` (принцип «один главный CTA»),
`docs/04-tasks-sonnet.md` (задача **T-D5**). Контекст: Тёма (владелец) не смог с главной
попасть на страницу проектов — навигация разная на разных страницах, это чиним.

## A. Единая шапка на ВСЕХ страницах

Одна и та же навигация: **Главная (index.html) · Проекты (projects.html) · О нас
(about.html) · [Вступить]** (слот `data-header-auth`, как сейчас на index).

- Страницы: `index.html`, `projects.html`, `submit.html`, `project.html`,
  `profile.html` (появилась в T-D4), `about.html`, `404.html`.
- С главной убрать якорные пункты `#showcase / #how / #community` из шапки. Сами
  секции и их `id` не трогать.
- Текущая страница в nav — визуально отмечена (класс `active` или `aria-current="page"`,
  заведи единообразно).
- На `about.html` и `404.html` шапка может быть совсем старой (студийной, с Telegram-CTA) —
  привести к общей. Контент этих страниц НЕ переделывать (about — задача T6), только шапку,
  футер и пункт C ниже.
- `js/app.js` подключить там, где ещё не подключён (иначе слот авторизации мёртвый).

## B. Путь к витрине с главной

- В `showcase-head` на главной добавить ссылку **«Все проекты →»** → `projects.html`
  (по `03-content.md`; справа от заголовка секции на десктопе, под ним на мобиле).
- Проверь и другие тупики: со страницы проекта и профиля должен быть очевидный путь
  назад к витрине (например «← Все проекты» над карточкой). Реши сам, но чтобы
  с ЛЮБОЙ страницы витрина достигалась в один клик.

## C. Telegram-ссылки и остатки фейка

- Убрать личную `t.me/androsovart` из community-кнопок на `about.html`, `404.html` и
  везде, где осталась (grep по репо). Замена: канал `https://t.me/wedesignerz`,
  чат `https://t.me/+98-s06KjbuNhM2Zi` — развести подписи «Telegram-канал» /
  «Чат сообщества».
- Исключение: hire-CTA «Начать проект» внутри старых дизайн-кейсов `projects/*.html` —
  НЕ трогать (это личка Тёмы для найма, переверстается в T6).
- «Вступить» (`[data-join]`) везде открывает модалку логина, не тост и не Telegram.
- Пройдись grep'ом по выдуманным цифрам/никам (`1 236`, `1 248`, `@tyoma` и т.п.) —
  на страницах вне `projects/*.html` фейка остаться не должно.

## Ограничения
- Токены, i18n-строки, ESM — как обычно. Футер уже единый на новых страницах — сверь,
  чтобы about/404 получили такой же.
- Не трогать: витрину, hero, how-terminal, ленту участников (T-D4), контент about.

## Проверка (критерии приёмки)
- Прокликай путь новичка: главная → «Проекты» в шапке → карточка → «← Все проекты» →
  профиль автора → назад. Нигде не застрять, шапка везде одна.
- `grep -rn "androsovart" --include="*.html" .` — пусто вне `projects/`.
- «Вступить» на каждой странице открывает модалку.
- Консоль чистая на всех 7 страницах; линтеры — 0 ошибок.

## После выполнения
Отчитайся кратко. Следующее — T5 (финальная пересборка главной) по плану.
